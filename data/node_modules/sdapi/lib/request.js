"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const node_fetch_1 = require("node-fetch");
async function translate(word) {
    if (!word.length) {
        return Promise.reject(new Error('Zero-length word'));
    }
    const res = await (0, node_fetch_1.default)(`https://www.spanishdict.com/translate/${word}`);
    return await res.text();
}
async function conjugate(verb) {
    if (!verb.length) {
        return Promise.reject(new Error('Zero-length word'));
    }
    const res = await (0, node_fetch_1.default)(`https://www.spanishdict.com/conjugate/${verb}`);
    return await res.text();
}
exports.default = {
    translate,
    conjugate
};
